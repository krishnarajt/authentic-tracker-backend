apiVersion: v1
kind: Service
metadata:
  name: authentic-tracker-backend
  namespace: authentic-tracker-backend
spec:
  type: NodePort
  selector:
    app: authentic-tracker-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 32011
